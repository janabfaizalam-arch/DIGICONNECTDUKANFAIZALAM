"use client";

/**
 * Optional provider alias — navigation loading lives in NavigationProgress
 * (mounted once from the root layout).
 */
export { NavigationProgress as NavigationLoadingProvider } from "@/components/navigation-progress";
