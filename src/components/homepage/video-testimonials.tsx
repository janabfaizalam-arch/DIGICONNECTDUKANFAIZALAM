/**
 * Video testimonials require admin-approved CMS records with consent.
 * Until that content source exists, this section stays hidden (no stock/fake videos).
 */
export function VideoTestimonials() {
  return null;
}
