import { IdeasWorkbench } from "@/components/content-engine/ideas-workbench";

export const dynamic = "force-dynamic";

/** Stage 01 — where next week's posts come from. */
export default function Page() {
  return <IdeasWorkbench />;
}
