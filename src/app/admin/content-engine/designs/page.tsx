import { DesignsWorkbench } from "@/components/content-engine/designs-workbench";

export const dynamic = "force-dynamic";

/** Stage 05 — a design brief complete enough to build from by hand. */
export default function Page() {
  return <DesignsWorkbench />;
}
