import { BrandWorkbench } from "@/components/content-engine/brand-workbench";

export const dynamic = "force-dynamic";

/** How DigiConnect sounds, derived from posts that actually worked. */
export default function Page() {
  return <BrandWorkbench />;
}
