import { RepurposeWorkbench } from "@/components/content-engine/repurpose-workbench";

export const dynamic = "force-dynamic";

/** Stage 06 — one idea, seven native versions. */
export default function Page() {
  return <RepurposeWorkbench />;
}
