import { AnalyticsWorkbench } from "@/components/content-engine/analytics-workbench";

export const dynamic = "force-dynamic";

/** What each post did, and what the Learn engine makes of it. */
export default function Page() {
  return <AnalyticsWorkbench />;
}
