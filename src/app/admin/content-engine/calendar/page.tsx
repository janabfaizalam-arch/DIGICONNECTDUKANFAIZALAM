import { CalendarWorkbench } from "@/components/content-engine/calendar-workbench";

export const dynamic = "force-dynamic";

/** The content calendar, and the weekly rhythm behind it. */
export default function Page() {
  return <CalendarWorkbench />;
}
