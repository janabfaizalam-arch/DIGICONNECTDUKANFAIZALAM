import { notFound } from "next/navigation";

export const dynamic = "force-dynamic";

/** CSC Olympiad CMS retired — service is not in the approved catalog. */
export default function CscOlympiadAdminPage() {
  notFound();
}
